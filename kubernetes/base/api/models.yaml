# The registry the API serves, mounted at /config/models.yaml via ConfigMap
# `api-models`. Same shape as api/models.yaml, with the in-cluster backend URL.
models:
  - name: qwen2.5:0.5b
    backend_url: http://ollama.openmodel-inference:11434
    capabilities: [chat, completion]
  - name: nomic-embed-text
    backend_url: http://ollama.openmodel-inference:11434
    capabilities: [embedding]
  # Served by the second backend: the registry is what routes per model, so a
  # new model is a Deployment plus this one URL. Only the pro plan grants it.
  - name: qwen2.5-coder:0.5b
    backend_url: http://ollama-coder.openmodel-inference:11434
    capabilities: [chat, completion]
